# Sunshower — Backlog Archive

Completed (✅) and resolved work, lifted out of [sunshower_backlog.md](sunshower_backlog.md) so the active backlog stays scannable for current-phase tasking. **This file is provenance, not a worklist** — nothing here is actionable. Section headers mirror the active backlog. For the chronological wiki record see [vault/log.md](../vault/log.md); this is the project-plan record of decisions shipped.

Archived 2026-05-18 on `feat/close-phase-1` (Phase-1 close-out). Append future ✅ items to the matching section here when they're moved out of the active backlog.

---

## Changelog (historical)

Running "Last updated" narrative from the active backlog, preserved here in full so the active doc can carry just a short current-state pointer.

- **2026-07-04** — **Site-inventory walkthrough MVP shipped** (`feat/site-inventory-mvp`): the "mostly bare" interstitial became an 8-step wizard + site-profile summary. Data layer (`SiteProfile` v1 contract in localStorage, `useSiteProfile` hydrate-after-mount hook), wizard shell (stepper-as-navigation, `?step=` deep-links, resume-to-first-open-step, venue chips), 8 vault-sourced step components (archetype key mini-flow, 5-tier sun-zone editor, `tel:811` action), summary card + Phase-2 bridge. Integration: cleanup-plan §3 → site-inventory pointer, care-page cross-link, phases.md repositioning (site inventory now sits between Phases 1 and 2, drawing tools stay Phase 3). Vitest added as the repo's first test runner (13 tests, `npm test`, added to CI). Spec + per-package build record: [sunshower_site_inventory_mvp.md](sunshower_site_inventory_mvp.md).
- **2026-05-18** — **Phase 1 (Cleanup) closed** on `feat/close-phase-1` — cleanup-plan Sections 2–3 shipped (prep + Phase-2 handoff), so the three-step User-zero MVP (Input → Confirm → Plan) is complete; the backlog was also split, completed work → this archive. Calscape native-plant ingest merged earlier (PR #19) — 150 SJ/Bay Area `nativity: native` stub pages; **Phase-2 data layer is live**. Remaining Phase-1 work is polish only (friendly-to-natives copy pass, picker-scope de-emphasis).
- **2026-05-17 (lint)** — **Vault lint pass** (`chore/vault-lint-post-calscape`): post-WRIC+Calscape punch list. The predicted "150 Calscape orphans + index drift" did **not** materialize — the rollout regenerated `index.md` programmatically so it stayed in sync (0 missing, 0 strict orphans, 0 frontmatter violations, 0 unresolved contradictions). Surfaced the real debt: **285 plant graph-orphans** (150 natives + 135 invasives reachable only from the flat catalog, zero peer links) + the **150-native `regions: []` gap**, both now backlogged under *Native plant ingest* / *Schema / wiki maintenance*. Committed reusable [`vault/scripts/lint_vault.py`](../vault/scripts/lint_vault.py); fixed 1 dangling index wikilink.
- **2026-05-17** — **Calscape (CNPS) native-plant ingest** pivoted to + piloted on `feat/calscape-ingest` (branched off main). Prompted by the realization that Luc's "ready to plant" = the full landscape-design + plant-selection arc (Phase 2), not the MVP's generic post-cleanup Section 2 — and the vault had design/soil concepts but **no native plant dataset**. Strategy resolved: region-scoped to SJ/Bay Area (Calscape location filter → 150 taxa, RPRP-correct). Key finding: Calscape's Excel export carries the full 50-column structured dataset in one request (no per-species scrape for the core). Pilot (6 taxa) → Luc approved the `native:` frontmatter block + normalization resolution → **full 150-taxa rollout shipped**: dataset meta-page [[sources/calscape]] (strategy artifact + Phase-2 selector model), committed tooling `vault/scripts/build_calscape_plant_pages.py`, 150 `nativity: native` stub pages, index.md `### Natives` catalog, log.md entries. Two new 📋 follow-ups queued: Calscape enrichment scrape (editorial prose + named lep hosts) + Phase-2 selector design. Agent D §2–3 (cleanup-plan prep-for-planting) deprioritized, not abandoned.
- **2026-05-16** — GUI-MVP doc closed out: its remaining open tasks were migrated into the backlog — Agent D §2–3 (prep-for-planting + coming-next placeholder) under *Cleanup-plan rendering → sections 2–3*; the post-DB *Deferred* set as a *Plan enrichment — post-DB* icebox entry under *App / UI*. [sunshower_gui_mvp.md](sunshower_gui_mvp.md) is now a closed per-agent build record (no new tasks there). Same day: Agent C shipped on `feat/sunshower-yard-state-router` (PR #16, merged): `/sunshower` now leads with a "How's your yard looking today?" dropdown that routes overgrown/partial → cleanup-plan (`?yard=`), bare → new `/sunshower/site-inventory` interstitial, established → new `/sunshower/care` interstitial; shipped as a dropdown (not the spec'd free-text matcher) + dedicated route pages (not inline wiki links). Plus a full responsive/mobile pass — desktop keeps the fixed immersive 3D scene, mobile is a scrollable doc with an in-flow shovel stage and a continuous ambient `backdrop` Scene variant. Unblocks Agent D's "prep for planting" section.
- **2026-05-16 (prior)** — Doc-lint pass on `chore/lint-stale-plant-counts`: synced stale "137 plants" claims in [PlantPicker.tsx](../src/app/sunshower/cleanup-plan/PlantPicker.tsx), [page.tsx](../src/app/sunshower/cleanup-plan/page.tsx), [plan.ts](../src/app/sunshower/cleanup-plan/plan.ts), and [planning/sunshower/resources.md](sunshower/resources.md) to the current 158-plant catalog. New App/UI item added: picker-scope de-emphasis + advanced-search toggle for non-residential invasives.
- **2026-05-16 (prior)** — Stepper-nav fix on `fix/cleanup-plan-stepper-nav` (PR #14, merged) — sync picks on back-nav, gated forward-nav. Earlier same day: Beginner-gardening wiki ingest on `feat/beginner-gardening-ingest` (PR #13): 4 sources, 5 new concepts (garden-zoning, wildlife-coexistence, planting-technique + composting/pruning stubs), 4 concept updates, planning-doc ideas section. New App/UI backlog items added for homepage yard-state router, in-app site-inventory walkthrough, parallel-tracks UX, friendly-to-natives copy pass, Phase 4 (Care) gap.
- **2026-05-15 (prior)** — iNat photo pull for the 21 new UC IPM residential weeds (`feat/inat-photos-ucipm-weeds`); 158/158 picker rows at 5 photos. `plants.json` wiring for the 22 weeds (PR #11, merged) — 21 new picker rows + Layer A/B/C/sources, `prune_host_below_attachment` enum for dodder.

---

## Cal-IPC data ingest

- ✅ Plan ingest pipeline (raw → wiki → Supabase) (2026-05-08)
- ✅ Top-tier scrape: 137 plants × 2 docs (profile + PAF) → `vault/raw/articles/calipc/` (2026-05-08)
- ✅ Top-tier wiki ingest: 137 plant pages, 1 curated + 136 stubs, with full invasive frontmatter (2026-05-08)
- ✅ Cross-cutting concepts filed: cal-ipc-scoring, vegetative-spread, horticultural-introduction-pathway (2026-05-08)
- ✅ Synthesis: [calipc-top-tier-overview](../vault/synthesis/calipc-top-tier-overview.md) — patterns + foothills-priority subset (2026-05-08)
- ✅ WRIC scrape — regional first pass: 7 PDFs (Cytisus, Foeniculum, Genista, Hedera, Rubus, Spartium, Ulex) landed in `vault/raw/pdfs/wric/` for foothills invasives that already have Cal-IPC profiles; full 274-PDF Box archive catalogued in `vault/raw/pdfs/wric/_index.md` with file IDs and a download recipe for future passes (2026-05-08). WRIC migrated reports to a Box folder while their site is "under construction"; legacy `wric.ucdavis.edu/information/natural areas/...` URLs that Cal-IPC pages link to all 302 to the placeholder now.
- ✅ **WRIC wiki ingest** — All 38 annotated plants now at `status: draft` with Identify / Remove / Prevent sections populated from WRIC + UC IPM Pest Notes. 28 unique WRIC files cover the inventory (8 multi-species, e.g. Hedera covers helix/canariensis/hibernica jointly); 2 congener citations (Acacia dealbata via A. melanoxylon; Cytisus striatus via C. scoparius); 1 plant (stinknet, *Oncosiphon pilulifer*) post-dates the 2013 WRIC book and uses county Ag Commissioner synthesis instead. PDF-reader blocker resolved by switching to firecrawl scraping the Box-hosted PDFs (recipe in [vault/scripts/scrape_wric.py](../vault/scripts/scrape_wric.py)). Pilot logged 2026-05-11 (5 plants); full rollout logged 2026-05-12 (remaining 33). `sources/wric.md` meta-page also landed. (2026-05-12)
- ✅ Photo download from Cal-IPC profiles: 540 images → `vault/raw/assets/calipc/<slug>/`, 137 plant subfolders, 140 MB total (2026-05-08). Plant pages do not yet reference these locally — wiring covered in the Photo sourcing section below.

## Photo sourcing (display-tier)

- ✅ iNaturalist photo pull for the 137 Cal-IPC invasives — license filter `cc0,cc-by`, cap 5/plant with one-photo-per-photographer, synonym resolution via iNat `matched_term` with parent-species fallback. 626 photos, ~275 MB → `vault/raw/assets/inaturalist/<slug>/{NN.jpg, metadata.json}`. Coverage: 118 full / 19 partial / 1 parent-fallback. Logged in [vault/log.md](../vault/log.md) under `[2026-05-08] ingest | inaturalist photo fetch`. (2026-05-08)
- ✅ **Synonym-resolution spot-check** — manually verified the 6 reclassified/fallback iNat taxa (`fallopia-japonica` → *Reynoutria japonica*; `pennisetum-setaceum` → *Cenchrus setaceus*; `eichhornia-crassipes` → *Pontederia crassipes*; `polygonum-sachalinense` → *Reynoutria sachalinensis*; `brassica-nigra` → *Mutarda nigra*; `centaurea-jacea-ssp-pratensis` parent-species fallback) by opening each photo set and matching diagnostic features against trusted references. All 6 confirmed. Side finding: ~14 plants resolved via the fetcher's second-pass synonym retry have `photos_taxon_id`/`photos_from_parent_species` as `None` (older schema) — non-blocking; a re-run would normalize. (2026-05-09)
- ✅ **Wire photos into plant pages** — `vault/scripts/wire_photos_into_plant_pages.py` inserts a `## Photos` section before `## Sources` in every `vault/plants/*.md`, with `### iNaturalist` (attribution + observation URL from `metadata.json`) and `### Cal-IPC` (photographer + year parsed from filename) subsections. Idempotent. All 137 pages populated (`triadica-sebifera` lacks Cal-IPC; `centaurea-jacea-ssp-pratensis` carries the parent-species note). (2026-05-09)
- ✅ **iNat partial top-up** — added `--allow-duplicate-users` flag to `vault/scripts/fetch_inaturalist_photos.py`. Two changes lifted the 19 stuck slugs: (1) relax the per-photographer dedup, (2) merge `popular=true` results with the unfiltered research-grade query (the original fetch only fell back to unfiltered when popular was empty — for partials, popular returned a few obs and we never reached the broader pool). Idempotent on `photo_id`. All 19 reached 5/5; 137/137 plants now at full coverage. (2026-05-09)

## Common weed ingest (UC IPM Residential Pest Notes)

- ✅ Scrape pipeline + 28 Pest Notes → `vault/raw/articles/ucipm-residential/`. 10 overlap with Cal-IPC plants (already cited inline in [[plants/cytisus-scoparius]] et al.); 22 cover residential weeds not in the Cal-IPC inventory. (2026-05-13)
- ✅ Scrape pipeline + 7 cross-cutting management docs → `vault/raw/articles/ucipm-general/` (soil solarization, lawn-weed mgmt, landscape weed mgmt, woody-weed invaders, rose-bed weeds, invasive-plants intro). (2026-05-13)
- ✅ Source meta-page [[sources/ucipm-residential]] documenting scope, document structure, citation, and nativity-handling for non-Cal-IPC weeds. (2026-05-14)
- ✅ **Pilot wiki ingest** — first 5 residential weeds (dandelion, field-bindweed, common-purslane, mallows, spotted-spurge) covering distinct biologies. Validated the template before the full rollout. (2026-05-14)
- ✅ **Full wiki ingest** — 16 remaining residential weeds + native-reference poison-oak (21 total new plant pages: annual-bluegrass, burning/stinging-nettles, catchweed-bedstraw, chickweeds, clovers, common-groundsel, common-knotweed, dallisgrass, dodder, green-kyllinga, kikuyugrass, plantains, pokeweed, puncturevine, russian-thistle, poison-oak). Plus dyer's woad (*Isatis tinctoria* — Cal-IPC stub upgraded to draft with UC IPM management content). (2026-05-14)
- ✅ **Sync new weeds to `plants.json`** — Layer A/B/C + `removal_sources` wired for all 22 (21 new + dyer's woad upgrade) via [vault/scripts/add_ucipm_weed_entries.py](../vault/scripts/add_ucipm_weed_entries.py) (new) plus extensions to the 4 existing apply/backfill scripts. Picker DB grew 137 → 158; annotated subset 38 → 60. New `RemovalMethod` enum value `prune_host_below_attachment` introduced for dodder (parasitic — the action targets the host, not the weed); the existing 10-method vocabulary covered everything else. Cal-IPC overlap plants (brooms × 5, yellow starthistle, poison hemlock, perennial pepperweed, Bermuda buttercup, Himalayan blackberry) now carry both WRIC + UC IPM citations. Vocabulary + per-plant roster: [[synthesis/invasive-removal-methods]]. (2026-05-15)
- ✅ **iNat photo pull for new weeds** — same recipe as the Cal-IPC 137-plant pull: `vault/scripts/fetch_inaturalist_photos.py` (CC0/CC-BY, research-grade + popular, 5/plant, one-photographer cap; salsola-tragus topped up via `--allow-duplicate-users`). 105 new photos for the 21 net-new picker rows. Wikimedia Commons fallback turned out *not* to be needed — cosmopolitan species had abundant CC contributors. New `vault/scripts/sync_inat_photos_to_plants_json.py` reflects iNat `metadata.json` into `plants.json` `photos[]` (4-field projection); same run picked up a pre-existing drift in the 137 Cal-IPC plants (3 → 5 photos each, post-2026-05-09 top-up that was never synced to plants.json). 158/158 picker rows now at 5 photos. (2026-05-15)

## Native plant ingest

- ✅ **Calscape (CNPS) ingest — full SJ/Bay Area rollout** (`feat/calscape-ingest`, 2026-05-17). Dataset meta-page [[sources/calscape]] = the strategy artifact. Region-filtered to SJ/Bay Area (`lat=37.3382,lng=-121.8863` → **150 taxa**), RPRP-correct, not 8,507 statewide. **Key finding:** the Calscape `/export/search/?lat=&lng=` Excel endpoint returns the *full* 50-column structured dataset for all 150 in **one request** — no per-species scrape for the core (Calscape `/search` is infinite-scroll JS). Tooling: [`vault/scripts/build_calscape_plant_pages.py`](../vault/scripts/build_calscape_plant_pages.py) (committed, stdlib-only, idempotent, `--lat/--lng` retargetable). Luc approved the `native:` frontmatter block (parallel to `invasive:`; resolves the open *cultivar-vs-species* + *microclimate* questions) + the normalization resolution (selector reads structured block; flat fields coarse; `Fern`→`perennial`+`plant_type_raw`; conservative `soil:`). 150 `nativity: native` stub pages, indexed under `### Natives` (tree 12 / shrub 20 / vine 3 / perennial 46 / annual 50 / grass 19). Mid-rollout fixes: lone-`.` float crash, fern mapping. **Merged via PR #19 (2026-05-18).**

## Schema / wiki maintenance

- ✅ `invasive:` frontmatter namespace defined in [vault/CLAUDE.md](../vault/CLAUDE.md) (2026-05-08)
- ✅ Dataset source-meta-page convention (one page per dataset, not per record) (2026-05-08)
- ✅ `raw/articles/<source-slug>/` subfolder convention for bulk-scraped sources (2026-05-08)
- ✅ **Lint pass after WRIC + Calscape ingests** (`chore/vault-lint-post-calscape`, 2026-05-17). Reusable read-only tool committed: [`vault/scripts/lint_vault.py`](../vault/scripts/lint_vault.py) (non-zero exit on hard defects — gateable in CI). Predicted "150 Calscape orphans + index drift" did **not** materialize: the rollout regenerated `index.md` programmatically → 0 missing pages, 0 strict orphans, 0 frontmatter violations, 0 unresolved contradictions. Fixed: 1 dangling wikilink (`[[feedback-sunshower-weed-scope]]` → unlinked, was an auto-memory filename never in the vault). Real debt surfaced → two live 📋 items in the active backlog (region cross-link under *Native plant ingest*; plant-tier graph hygiene here under *Schema / wiki maintenance*). Full punch list: [vault/log.md](../vault/log.md) 2026-05-17 lint entry.

## App / UI

- ✅ **User-zero MVP — pick list → confirmed list → attack plan** (Phase 1 cleanup-plan flow, completed 2026-05-18 on `feat/close-phase-1`). [/sunshower/cleanup-plan](../src/app/sunshower/cleanup-plan/page.tsx), three steps: **(1) Input** — autocomplete picker over the Cal-IPC + UC IPM DB (PR #4, Agent A, 2026-05-11); **(2) Confirm** — per-pick photo verification + lightbox + keep/drop (Agent A); **(3) Plan** — yard-wide summary (Layer B) + method-grouped action plan (Layer A) + Sections 2–3 (prep + coming-next). All three steps shipped; the Plan step's data layers + Sections 2–3 are detailed under *Cleanup-plan rendering* below. Out of MVP scope (still icebox in the active backlog where relevant): in-app photo recognition, satellite/yard map overlay, multi-yard accounts, login. Per-agent build record: [planning/sunshower_gui_mvp.md](sunshower_gui_mvp.md). Phase-1 polish items (friendly-to-natives copy pass, picker-scope de-emphasis) remain separate 📋 items in the active backlog — they're enhancements, not MVP-core.

### Beginner-gardening ideas

- ✅ **In-app site-inventory walkthrough** (`feat/site-inventory-mvp`, 2026-07-04). [vault/concepts/site-inventory.md](../vault/concepts/site-inventory.md) exposed as an 8-step guided wizard at `/sunshower/site-inventory` — Rainer & West archetype key ([vault/concepts/landscape-archetypes.md](../vault/concepts/landscape-archetypes.md)) opens the flow, then aspect (cardinal picker + optional compass bearing), sun map (hourly-photo prompt → named zones on the 5-tier vocabulary), wind, water & slope, utilities (`tel:811`), sightlines, soil/pH indicator-plant prompt (*"see any rhodos or lavenders nearby?"*). Ends in a persistent site-profile summary (versioned localStorage, maps 1:1 to a future Supabase `site_profiles` table). Venue tags shipped as the parallel-tracks down-payment. Spec + build record: [sunshower_site_inventory_mvp.md](sunshower_site_inventory_mvp.md). Idea source: [vault/sources/gardenersworld-soil-ph.md](../vault/sources/gardenersworld-soil-ph.md) + [vault/sources/summerwinds-sun-exposure.md](../vault/sources/summerwinds-sun-exposure.md).
- ✅ **Homepage yard-state router — "How's your yard looking today?"** (PR #16, 2026-05-16). Phase-routing control on `/sunshower`, shipped as a 4-option dropdown (the spec'd free-text + keyword matcher was dropped — a dropdown removes matcher ambiguity) with an idle cycling label. Yard state is now the app's front door, not a cleanup-plan-internal input. Routes: overgrown → `cleanup-plan?yard=overgrown`, partially planted → `cleanup-plan?yard=partial` (selective-cleanup framing), mostly bare → new `/sunshower/site-inventory` interstitial, established → new `/sunshower/care` interstitial. The bare/established destinations shipped as dedicated "coming soon" route pages (matching cleanup-plan's visual vocabulary) rather than inline wiki-link interstitials — they now give the full site-inventory walkthrough + Phase 4 (Care) backlog items a real landing surface. New [yardState.ts](../src/app/sunshower/yardState.ts) (`YardState` + `routeForYardState`), [YardStateRouter.tsx](../src/app/sunshower/YardStateRouter.tsx), [SunshowerLanding.tsx](../src/app/sunshower/SunshowerLanding.tsx). Unblocked Agent D's "prep for planting" section. Per-agent detail: [sunshower_gui_mvp.md → Agent C](sunshower_gui_mvp.md).

### Cleanup-plan rendering

The MVP Plan step's three data layers (sequenced A → C → B) + the three rendered sections — **all complete (2026-05-18).** Section 1 = grouped removal + yard-wide summary; Sections 2–3 = prep + Phase-2 handoff.

- ✅ **Sections 2–3 — "Prep the area for planting" + "Coming next"** (completed 2026-05-18 on `feat/close-phase-1`; migrated from GUI-MVP Agent D §2–3, unblocked by Agent C PR #16). [page.tsx](../src/app/sunshower/cleanup-plan/page.tsx): **(2) `PrepSection`** — generic, yard-state-driven guidance (dispose-don't-compost, leave established trees/keepers alone, let the seedbank flush, what "ready to plant" looks like); `partial` vs `overgrown`/default copy variants off the `?yard=` param; the only data touch is a `hasFragmentSpreader` check that strengthens the disposal line. **(3) `ComingNextSection`** — dashed/muted Phase-2 handoff placeholder (native selector over the 150 Calscape species, region-aware planting timing, per-site soil prep), honest that Selection is in development; links back to `/sunshower`. **Anti-goal honored:** no per-plant detail card — both sections are generic prose/checklist. tsc + eslint + `next build` clean.

- ✅ **Layer A — Wire `removal_method` + `removal_notes[]` into the cleanup-plan render.** `buildPlan` now returns `MethodGroup[]` (`{ method, methodLabel, plants: { plant, notes }[] }`) bucketing on `plant.removal_method`; the spread-mechanism `methodFor` heuristic is gone. `PlanSection` renders one card per action — method label as the heading, plants listed under it with photo thumb, Cal-IPC badge, and `removal_notes[]` as bullets. Group order: worst Cal-IPC severity in group → group size desc → method key alpha; null-method plants sink to a single "Method not yet documented" group with `spread_mechanisms`-derived fallback bullets. Three brooms now collapse to one shared action card. (2026-05-12)
- ✅ **Layer B — Top-level cleanup summary above the per-plant cards.** New `removal_timing_window`, `requires_followup_years`, `safety_flags[]` schema fields populated for the 38 annotated plants via [vault/scripts/apply_layer_b_fields.py](../vault/scripts/apply_layer_b_fields.py). `buildSummary` in [plan.ts](../src/app/sunshower/cleanup-plan/plan.ts) returns four buckets — tools (union of per-method tool lists + universal landfill-bag line), timing (plants bucketed by window, sorted Jan→Dec), cautions (closed-vocabulary `SafetyFlag` → human-readable yard-wide warning; `fragment_spreader` escalates at 2+ plants), and multi-year follow-up (≥5-year horizons). Rendered above the action plan as a 2×2 grid of `SummaryCard`s in [page.tsx](../src/app/sunshower/cleanup-plan/page.tsx). (2026-05-13)
- ✅ **Layer C — Refresh `removal_notes[]` in `plants.json` from the WRIC + UC IPM wiki rollout.** All 38 annotated plants regenerated via [vault/scripts/apply_layer_c_notes.py](../vault/scripts/apply_layer_c_notes.py) — concrete herbicide dilutions (e.g., Garlon 4 Ultra 1:4 in water for brooms; chlorsulfuron 1–2.6 oz/acre for pepperweed; undiluted glyphosate cut-stump for Arundo within 1–2 min), timing windows (Garlon foliar on fennel late Feb–early March; flower-bud-stage mow-then-spray for pepperweed; 8% v/v low-volume glyphosate for the Cortaderia pair after flowering), and what-doesn't-work warnings (don't mow Hedera/Vinca/Delairea/Clematis — fragments regenerate; don't burn brooms/gorse alone — fire triggers seedbank; don't till pepperweed — fragments propagate). Bullets average 4.8 per plant (range 4–5). Same idempotent DATA-dict pattern as Layer B. (2026-05-14)

## Scrapaholic retirement

- ✅ Cut `archive/scrapaholic` branch from `main` at the pre-removal state (full code + Prisma schema + planning docs intact). Pushed to origin. (2026-05-10)
- ✅ Removed `/scrapaholic` route, API handlers, lib modules, Prisma models, and middleware from `main` (commits `dd33e86` + follow-up `fbac3e8`). Package.json deps for Google AI, Firecrawl, Prisma, pg all dropped. (2026-05-10)
- ✅ CLAUDE.md no longer mentions scrapaholic — clean break rather than an "archived" pointer; archive branch is the only reference. (2026-05-10)

## Tooling

- ✅ firecrawl scrape pipeline: `build-jobs.sh`, `run-scrape.sh` (2026-05-08)
- ✅ Plant page generator: `build-plant-pages.py` — parses Cal-IPC PAF + profile, emits plant pages with frontmatter (2026-05-08)
- ✅ Aggregate stats: `aggregate-stats.py` — cross-plant statistics for synthesis pages (2026-05-08)
- ✅ iNaturalist photo fetcher: `vault/scripts/fetch_inaturalist_photos.py` — scientific-name lookup with `matched_term` synonym resolution + parent-species fallback, license/quality-grade filters, per-plant cap with one-photo-per-photographer, idempotent (2026-05-08)
- ✅ Committed-pipeline home settled at `vault/scripts/` — scrape_ucipm.py, scrape_wric.py, build_calscape_plant_pages.py, photo/apply/backfill helpers, lint_vault.py all live there; `.firecrawl/` demoted to gitignored scratch. Closes the old "promote scripts out of .firecrawl" question for everything except the legacy Cal-IPC one-offs (still 📋 in the live backlog) (2026-05-17)

## Documentation

- ✅ [Project home — planning/sunshower/](sunshower/) (split 2026-05-11 from the original [sunshower.md](sunshower.md) outline)
- ✅ [Vault schema (vault/CLAUDE.md)](../vault/CLAUDE.md)
- ✅ The backlog itself

## Open questions — Resolved (2026-05-09)

- ~~User-zero workflow~~ → resolved: paste plant-name list (iNaturalist is the offline ID tool) → photo-confirm each entry → generate attack plan. Tracked under **App / UI**.
- ~~Scrapaholic in active repo~~ → resolved: move to a `scrapaholic-archive` branch, strip from `main`, retire links. Tracked under **Scrapaholic retirement**.
- ~~Git tracking for `vault/raw/assets/` binaries~~ → resolved: gitignore image binaries (~415 MB total: 140 MB Cal-IPC + 275 MB iNat — easily regenerable via `vault/scripts/fetch_inaturalist_photos.py`), commit everything else including `metadata.json` sidecars (tiny; encode the legally-required attribution + source observation IDs needed for reproducibility and license compliance). Patterns added to `.gitignore`: `vault/raw/assets/**/*.jpg`, `**/*.JPG`, `**/*.jpeg`, `**/*.png` (mixed-case `.jpg`/`.JPG` covers Cal-IPC's case-inconsistent filenames). Same pattern applies to any future asset source. Independent of the eventual Supabase question — when Supabase comes online, photos will likely move to Supabase Storage / S3, not git.
