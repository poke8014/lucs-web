# Sunshower — Backlog

Coarse-grained task tracker for the [Sunshower project](sunshower.md). Domain-level entries, not individual tickets. The goal is multi-agent / multi-session coordination — one place to see what's in flight, queued, and blocked.

**Completed work lives in [sunshower_backlog_archive.md](sunshower_backlog_archive.md)** — this file carries only 🚧 / 📋 / 🧊 / ❓ so it stays scannable for current-phase tasking. The archive also holds the full historical "Last updated" changelog.

**Current state (2026-07-07):** **The M3 bed planner is BUILT — `/sunshower/bed-planner` (units A–I on `feat/bed-planner`, PR open; [spec = build record](sunshower_bed_planner_spec.md)).** It closes **M1** (base map: three on-ramps incl. privacy-guarded best-effort address import) and **M3** (paths-first sectioning, density style, phase order), and lands the spec'd M5/M6 slices (season scrubber + bloom overlay; per-section quantities/cost/effort), the 13-rule advisory checker, layered placement with per-plant suggested kinds, the **sun/shade seasonal timelapse** (S0 spike passed — NOAA math within ~1°, 0.07 ms scrub, fixed South Bay latitude), IndexedDB multi-plan persistence + fork + export/import, mobile read mode, and all entry points. Follow-ups queued: **grown-in 3D view** (🧊 App/UI), sociability backfill (upgrades the placement-kind heuristic in place), 🟡 good-neighbor set (🧊, selector unit E). **Natives display-photo pull ✅ done 2026-07-07** (`feat/plant-photos-inat` — iNat `cc0,cc-by`, 145/150 natives at 5 photos, `plants.json` records-with-photos 159→306; 5 narrow infraspecific taxa left for the Wikimedia fallback → archive under *Photo sourcing*). One day earlier, **the M4 plant selector shipped — `/sunshower/plant-selector` (units A–D, natives-only MVP; build record in [the spec](sunshower_plant_selector_spec.md))**; its regenerated data layer (`plants.json` 159→309, the 150 natives in the app layer — cleanup-plan picker tiering still 📋 under App/UI) and exported palette seam (`paletteSeam.ts`) fed the planner's unit C directly. Phase 1 (Cleanup) closed 2026-05-18; Phase-2 data layer live (150 Calscape native stubs, PR #19); the site-inventory walkthrough shipped 2026-07-04 (`feat/site-inventory-mvp` → versioned localStorage `SiteProfile`). The **milestone spine M0–M7** (below) is the roadmap: **M2 site-inventory ✅ shipped** (as the light walkthrough; the heavier computed-sun "living inventory" is now a deferred *upgrade*, not the next build); **M3 (paths/sections/density) has a build spec** — [sunshower_bed_planner_spec.md](sunshower_bed_planner_spec.md), which also absorbs **M1** (yard base map, incl. Luc's 2026-07-05 privacy-guarded address import) and the plan checker; **M4 (per-section selection) has a build spec** — [sunshower_plant_selector_spec.md](sunshower_plant_selector_spec.md), with plant-first entry as a first-class on-ramp. Both specs share one `plants.json` regeneration (selector unit A / planner unit C) — the natural next functional build. **Selector sign-offs resolved 2026-07-05:** ⚠️1–3 (route shape, build-now-without-enrichment, dismissal memory) approved → **units A–D built and shipped 2026-07-06** (→ archive under *Native plant ingest*); ⚠️4 (🟡 good-neighbor set) deferred until more plant sources are scraped (→ 🧊 under *Native plant ingest*). **Bed-planner sign-offs resolved 2026-07-06:** 2D-edit spine + placement kinds approved (placement kind now *suggested* per plant — social → drift/matrix, stand-alone → individual); at sign-off the **grown-in 3D view was separated out to its own item** (🧊 App/UI below) and the **sun/shade seasonal timelapse pulled in** as unit H (MVP = A–E+H+I; the previously-deferred S0 shadow spike opens the unit); picker tiering confirmed backlog-only. Phase-4 Care and visible-path navigation explicitly held for now (Luc 2026-07-05). Committed direction underneath (Luc 2026-07-03, corroborated by the r/NativePlantGardening ingest now in the vault): **per-section not whole-yard**, **density style** (landscaped vs. naturalistic), **saved plans** with jump-between-steps + outcome log, positioning = **the overlap of garden design and native-plant knowledge**; canonical text in [sunshower/CONTEXT.md → Direction set 2026-07-03](sunshower/CONTEXT.md#direction-set-2026-07-03--density-sectioning-saved-plans). Regional granularity confirmed **ecoregion-level** (Luc 2026-07-05). This state, the two specs, the Post-Wild World design theory (PR #24), and the reddit planning-flow ingest were rebased onto main together (the `docs/phase-specs` cleanup). Still in sequence under *Native plant ingest*: Calscape enrichment scrape (in flight, `feat/calscape-enrichment`). Known vault debt: 285 plant graph-orphans + 150-native `regions: []` (queued). Remaining Phase-1 work is polish only. Data-layer note (2026-07-06): the `plants.json` removal overlay was migrated into plant-page `removal:` frontmatter and the four-applier regen chain retired — **`plants.json` regen is now one command** (`node ops/build-plant-data.mjs`), no post-steps (`chore/removal-frontmatter-migration` → archive under *Schema / wiki maintenance*). Prior milestones: see the archive changelog.

## Conventions

| Status | Meaning |
|--------|---------|
| 🚧 | In progress — name the owner (agent / person) and what they're doing |
| 📋 | Up next — ready to pick up |
| 🧊 | Icebox — future / low priority / not yet shaped |
| ❓ | Blocked or open question — note what's blocking |
| ✅ | Done — moved to [sunshower_backlog_archive.md](sunshower_backlog_archive.md) (with completion date) |

**When picking up a task:** change 📋 → 🚧 and add owner.
**When finishing:** change 🚧 → ✅, add the date, and move the line to the matching section in [sunshower_backlog_archive.md](sunshower_backlog_archive.md). Keep this file free of standalone ✅ items (the one exception: ✅ sub-steps inside an in-flight 🚧 composite, where they give necessary structure).
**When adding a task:** append at the bottom of its domain section. Update **Current state** at the top.

Backlog ≠ wiki log. Wiki operations (ingests, refactors, queries) are logged in [vault/log.md](../vault/log.md) — that's the chronological record of what was filed in the wiki. This backlog is the project plan: what we've decided to do, what's in flight, what's queued.

---

## Roadmap — planning-flow milestones (added 2026-07-03)

Luc's ordering (2026-07-03): the community's shared planning sequence ([vault/synthesis/garden-planning-flow-signals.md](../vault/synthesis/garden-planning-flow-signals.md), signal 2) is the roadmap's milestone spine — each step is a milestone, and the other thread signals attach as features of the step they serve. **Positioning across every milestone: Sunshower shines in the overlap of garden design and native-plant knowledge** (the design-education gap, signal 6) — each step carries just enough design guidance that the output reads as intentional, not messy.

| # | Milestone | Features it encompasses | Status |
|---|---|---|---|
| M0 | Cleanup plan (Phase 1) | ID → triage → grouped removal plan | ✅ shipped 2026-05-18 (→ archive) |
| M1 | Yard base map | address-import / satellite-GIS trace / drawn canvas; dimensions → square footage; obstruction outlines; sections drawn | ✅ **shipped 2026-07-07 with the bed planner** (its unit B: three on-ramps incl. privacy-guarded address import → archive under *App / UI*) |
| M2 | Site-inventory (sun / water / soil / utilities / keepers / sightlines) | 8-step guided walkthrough → versioned `SiteProfile`; sun captured as a labeled zone list | ✅ **walkthrough shipped** 2026-07-04 (`feat/site-inventory-mvp`). Deferred upgrade: 🧊 *computed-sun "living inventory"* (per-datum confidence) — see *App / UI → Site-inventory computed-sun upgrade* below |
| M3 | Paths, zones & phase order | paths-first decomposition; sectioning + per-section phase state; density style | ✅ **built 2026-07-07** (`feat/bed-planner`, units A–I; [spec = build record](sunshower_bed_planner_spec.md) → archive under *App / UI*) |
| M4 | Per-section plant selection | Phase-2 selector; **plant-first entry**; confidence-banded fit verdicts; native/benign/invasive tiering | ✅ **units A–D shipped 2026-07-06** (`feat/plant-selector` → `/sunshower/plant-selector`; natives-only MVP). Build record in [the spec](sunshower_plant_selector_spec.md); 🟡 rung activates with unit E (🧊 under *Native plant ingest*) |
| M5 | Bloom / forage calendar | month-by-month bloom view; pollinator forage-gap detector | 🧊 *App / UI → Garden-planning-thread ideas* |
| M6 | Quantities & sourcing | area ÷ spacing at density → plant count → cost/effort; garden recipes / starter kits; nursery finder | 🧊 |
| M7 | Plant, log, revise | saved plans (auth + user tables); outcome log; next-season weighting | 🧊 behind Supabase — *App / Database* + *Authentication / saved garden plans* |

Cross-cutting: the **plan checker** (heuristic flags, not auto-layout) spans M3–M5; **parallel-tracks UX** (indoor mapping + outdoor removal) spans M0–M2. Entry is free even though the order is canonical: a plant-first user lands at M4 with M1–M2 data missing, and the fit verdict degrades gracefully to confidence bands while the app asks for the inventory it needs. (M0 stays in this table as the spine's anchor; its items live in the archive.)

## Cal-IPC data ingest

The California Invasive Plant Council Inventory — see [vault/sources/calipc.md](../vault/sources/calipc.md). Top-tier scrape + wiki ingest + WRIC management layer + photos are done (→ archive).

- 📋 WRIC scrape — broader passes as needed (remaining 267 PDFs in the archive). Recipe in `vault/raw/pdfs/wric/_index.md`. Pull on demand once Cal-IPC tier expands or specific species come up.
- 📋 Cal-IPC Limited tier scrape + ingest (~89 additional plants × 2 docs).
- 📋 Cal-IPC Watch tier scrape + ingest (~105 PRE pages — *different schema* from PAF; will need a separate parser pass).
- 📋 Cal-IPC Species ID Cards — separate PDF asset on Cal-IPC for a subset of plants. Designed for visual yard identification; high-value for user-facing app.

## Photo sourcing (display-tier)

Two-tier strategy: **research photos** (already in `vault/raw/assets/calipc/` from Cal-IPC — high botanical quality, but unclear redistribution rights) stay in the wiki for reference and never reach end users. **Display photos** (permissively-licensed, app-facing) are pulled separately from sources with explicit commercial-use OK and stored alongside attribution metadata. iNat pull + page-wiring + top-up are done (→ archive).

- 📋 Wikimedia Commons fallback — for plants where iNat top-up still leaves gaps (genuine CC photographer scarcity). Wikimedia tends to have consistently clean licensing and good botanical reference shots; smaller but complementary coverage to iNat. **Concrete queue after the 2026-07-07 native pull (→ archive):** 5 narrow infraspecific taxa with no CC photos at rank — `equisetum-hyemale-ssp-affine` (0), `poa-secunda-ssp-secunda` (0), `verbena-lasiostachys-var-lasiostachys` (0), `eryngium-aristulatum-var-aristulatum` (1), `heterotheca-oregona-var-scaberrima` (4). Several have a parent species that *does* have iNat photos, so a parent-rank fallback (or the species-page photo) is a cheaper alternative to Wikimedia for these.
- 🧊 Photo attribution UI surface — when users see a photo in the app, the photographer name + license needs to render. Out of scope until UI work begins; flagging now so the data layer captures attribution from the start (already captured in iNat `metadata.json`).

## Common weed ingest (UC IPM Residential Pest Notes)

Project-level scope expansion (2026-05-14): cleanup-plan covers **plants gardeners want out of their yard**, not just Cal-IPC-rated invasives. The average gardener doesn't sort by invasive vs. not. See [vault/sources/ucipm-residential.md](../vault/sources/ucipm-residential.md) for the dataset meta-page. Scrape + full wiki ingest + `plants.json` sync + photos are done (→ archive).

- 🧊 **Cross-cutting concepts page: "disturbed-soil weeds — what they're telling you"** — Russian thistle, puncturevine, knotweed, kikuyugrass, dyer's woad, dodder all have UC IPM management notes that point to upstream soil conditions (compaction, bare disturbed soil) as the fix. Pattern noted in the 2026-05-14 rollout log; promote to a concepts page when a few more plants accumulate.
- 📋 **UC IPM Weed Gallery — close the common-garden-weed gap** (raised 2026-05-17, query thread w/ Luc). The 28 Residential Pest Notes are *deep* but *narrow*; common garden/lawn weeds Luc actually finds — prickly lettuce (*Lactuca serriola*), California burclover (*Medicago polymorpha*), hedge mustard (*Sisymbrium officinale*), hawksbeard (*Crepis* spp.), sowthistle, filaree — have **no plant page**. Cal-IPC misses them by design (wildland-only); the Pest Note set didn't include them. **Primary target: the UC IPM Weed Gallery** (`ipm.ucanr.edu/PMG/WEEDS/<slug>.html`) — a separate, broader (~200 weed) photo-driven ID + quick-control DB; the breadth complement to the Pest Notes' depth. Same UC ANR pedigree → [[sources/ucipm-residential]] conventions (citation pattern, `nativity: invasive` minus the Cal-IPC block) extend unchanged. Scrape path **already proven**: the gallery URL pattern is directly observed in our scraped raw (e.g. `california_burclover.html`, `little_mallow.html`, `broadleaf_plantain.html` linked from `ucipm-residential/clovers.md`) and [`vault/scripts/scrape_ucipm.py`](../vault/scripts/scrape_ucipm.py) already renders `ipm.ucanr.edu` cleanly via firecrawl — likely a small extension, not new infra. Pages land `stub`/`draft` (gallery entries are lighter than Pest Notes — correct granularity for minor weeds). **RPRP scoping discipline (mirror Calscape):** "common garden weeds" is unbounded — scope to South Bay / Santa Clara County, *not* the whole ~200. Build the candidate list from Calflora's county weed filter + Luc's named weeds; do not ingest statewide. Sub-items:
  - 🧊 **WRIC backlog is the cheapest partial win — ingest, not scrape.** ~250 WRIC reports already converted to markdown in [`vault/raw/articles/wric/`](../vault/raw/articles/wric/) (incl. `Lactuca.md`, `Medicago.md`, `Sisymbrium_altissimum-irio.md`); for these the gap is *ingest*, covered by the existing "WRIC scrape — broader passes" item above. Caveat: WRIC is the *natural-area* (DiTomaso) management tier — acre-scale, wildland-skewed, wrong audience for a homeowner per [[sources/ucipm-residential]]'s own framing. Use as the removal layer *behind* a UC IPM Weed Gallery ID layer, not the primary garden-weed source. *Crepis* (hawksbeard) is absent from WRIC too — needs the gallery scrape.
  - 🧊 **Calflora for prioritization, not content** — folds into the existing `🧊 Calflora / Jepson Interchange` item under *Native plant ingest*. Calflora's Santa Clara County occurrence filter answers *which* weeds are actually in Luc's area (the region-scoping input above); it is range/occurrence data, not ID-and-removal narrative, so it scopes the gallery scrape rather than substituting for it. CDFA Encycloweedia (regulatory noxious weeds) is too narrow for the garden-weed gap — not pursued.

## Native plant ingest

The "what to plant instead" half of [[concepts/right-plant-right-place]] — Phase 2 plant selection. Full Calscape SJ/Bay Area rollout (150 taxa) merged via PR #19 (→ archive); the two items below are the live Phase-2 sequence.

- 📋 **Calscape enrichment scrape** — per-species page-scrape for the two fields the export omits: full editorial "About" prose + *named* butterfly/moth host species (export gives only a count). Upgrades the 150 pages `stub→draft` and seeds `host_plant_for` + the Pollinators index. ~150 firecrawl scrapes; recipe/format in [[sources/calscape]].
- 📋 **Region cross-link for the 150 natives — ride with the enrichment scrape** (raised by the 2026-05-17 lint pass). All 150 Calscape pages carry `regions: []`; only `regions/central-west.md` exists, so the region dimension the schema + [[concepts/right-plant-right-place]] depend on is unbuilt. The export was SJ-scoped (`lat=37.3382,lng=-121.8863`), so a single Bay Area / Santa Clara County region hub is well-defined. Do it as part of the enrichment pass (not a separate scrape): create the region hub page + set each native's `regions:` frontmatter to link it (extend [`build_calscape_plant_pages.py`](../vault/scripts/build_calscape_plant_pages.py) or the enrichment script). De-orphans 150 pages with a real second link in one move.
- 🧊 Calflora / Jepson Interchange — secondary native plant data sources.
- 🧊 Bloom-time data — Calscape export carries `Flowering Season` (now in `bloom_season` frontmatter); cross-check vs Jepson when bloom-succession planning starts.
- 🧊 **Good-neighbor (🟡 `non_native_safe`) starter set — selector unit E, deferred (Luc 2026-07-05).** The [selector spec](sunshower_plant_selector_spec.md)'s ⚠️4: a `benign:` frontmatter block + ~20–30 curated South Bay garden classics (lavender, rosemary, star jasmine, citrus, camellia…) with hand-picked `native_analogs`. Luc's call: **wait until more plants are scraped before assembling the curation — we need more sources first** (candidate citable bases: UC ANR / Xerces pollinator-friendly-ornamental lists). Until then the selector ships natives-only (MVP A–D); the 🟡 ladder rung auto-activates when these pages land. `benign:` schema shape still needs the vault-convention discussion before writing.

## Schema / wiki maintenance

Frontmatter namespace + dataset/source conventions are defined (→ archive).

- 📋 Phase-1 management concepts (removal-techniques, reinfestation-prevention, fire-risk, toxicity-handling, disposal) — unblocked by the 2026-05-12 WRIC rollout; cross-cutting content from the plant pages can now be lifted into standalone concept pages.
- 🧊 Phase-2 selection concepts (pollinator-value-types, bloom-succession, drought-tolerance-classes, cultivar-vs-species, plant-roles) — defer until Phase 1 cleanup workflow is functional. *(2026-07-03: bloom-succession now exists — [vault/concepts/bloom-succession.md](../vault/concepts/bloom-succession.md), from the reddit planning-thread ingest.)*
- 🧊 **`sociability` field — data gap flagged 2026-07-03.** Experienced gardeners' planning spreadsheets sort by sociability (how strongly a plant spreads/clumps — Rainer-West-style rating); no frontmatter field carries it and `native.plant_type_raw` doesn't cover it. Matters doubly for the naturalistic density mode (spreaders pack differently than clumpers). Add when a source can supply values — candidate: a CA-native design source ingest (also needed for naturalistic spacing numbers, see [sunshower/phases.md → Phase 3 open questions](sunshower/phases.md)).
- 🧊 Region pages beyond Central West (Northwest, Great Valley, Sierra Nevada, Southwest) — backfill on demand when foothill scope expands.
- 📋 **Plant-tier graph hygiene — 285 graph-orphans.** Every plant page (150 Calscape natives + **135 invasives**) is reachable only from the flat `index.md` catalog; zero peer-to-peer wikilinks (native→native body links: 0). The natives' fix is the Calscape enrichment scrape + region hub (tracked above — `companions` graph + `regions:` give them real peer links). The **135 invasive orphans have no de-orphaning path tracked anywhere** — candidate cross-link layers: shared `invasive.spread_mechanisms`/`habitat_types` → concept pages, the Phase-1 management concepts (item above), or [[synthesis/calipc-top-tier-overview]] back-links. Shape before scheduling. Re-run `lint_vault.py` to measure progress.

## App / Database (Supabase)

The structured layer — vault frontmatter is the source of truth; Supabase is derived.

- 📋 Supabase project setup (Postgres + auth)
- 📋 `plants` table schema mirroring shared + native-relevant frontmatter fields
- 📋 `invasive_assessments` table schema mirroring `invasive:` block (joined to plants on plant_id)
- 📋 Frontmatter → Postgres seed script (`vault/plants/*.md` → `plants` + `invasive_assessments` rows)
- 🧊 Wikilink resolution: `regions: [[regions/bay-area]]` → foreign keys to a regions table
- 🧊 `pollinators`, `host_plant_for` join tables (when native plants are ingested)
- 🧊 **User-data tables — saved garden plans (added 2026-07-03).** `users → yards → sections → section_plants` + per-plant outcome log; the only *stateful* data in the system (plant/region tables stay derived + rebuildable from vault frontmatter). Sketch in [sunshower/tech-stack.md → User data](sunshower/tech-stack.md). Pairs with the *Authentication / saved garden plans* item under App / UI; sequenced behind Supabase project setup.

## App / UI

The **User-zero MVP** (Phase 1 cleanup-plan: Input → Confirm → Plan, incl. Layers A/B/C + Sections 2–3) shipped 2026-05-18 → archive. Items below are Phase-1 polish + later-phase / cross-cutting UI.

- 📋 **Picker scope — de-emphasize non-residential invasives in autocomplete (added 2026-05-15).** The 98 Cal-IPC plants without a `removal_method` are all aquatic, salt-marsh, coastal-dune, or remote-rangeland species — explicitly "outside the residential cleanup-plan scope" per [vault/index.md](../vault/index.md), but currently rank equally with residential weeds in [src/app/sunshower/cleanup-plan/PlantPicker.tsx](../src/app/sunshower/cleanup-plan/PlantPicker.tsx) autocomplete. Approach: keep them findable but visually de-emphasized in default results (greyed thumb / muted text), and add an "Include rare invasives" advanced-search toggle that promotes them back to equal ranking. Likely needs a derived `residential_scope` field on each plant in `src/data/plants.json` (or for now, compute from `removal_method !== null`) so the picker can tier without re-deriving each keystroke. Pairs with the friendly-to-natives copy pass below — non-residential invasives shouldn't ride the same urgency framing as residential weeds. *(2026-07-06: the natives are now in the pool — the shared regeneration shipped with the selector, `plants.json` 159→309 — so tiering gains a second dimension: surface natives as "keep" candidates (native / benign / invasive), not just de-emphasize non-residential invasives. Luc confirmed this stays a backlog item, not bed-planner scope.)*
- 🧊 In-app identification flow — user uploads a photo or describes plant; app does the matching (replaces the offline iNat step in the MVP above).
- 🧊 Plant selection UI — given site conditions, suggest native plants (Phase 2).
- 🧊 **Plan enrichment — post-DB (migrated from the GUI-MVP *Deferred* list, 2026-05-16).** Deferred until the plant DB carries the needed fields; this is what the "Coming next" placeholder (Cleanup-plan rendering §3) fills in from: CA planting-window timing (zone-based), soil-prep recommendations (per plant / per region), best-time-to-mulch (per region), 3-tier native classification (native / benign / invasive) surfaced in the plan, site-context inputs beyond yard state (sun / soil type / region / water), and iNat API import (username/URL → fetched observations). Related items already tracked separately: the Phase-2 plant-selection bridge → "Plant selection UI" above; native-data ingest → *Native plant ingest*; in-app photo ID → "In-app identification flow" above.
- 🧊 **Authentication / saved garden plans (expanded 2026-07-03).** One user → many yards → **sections**; the plan persists across sessions so a user returns over weeks/seasons to add sections, adjust selections, and jump between phase steps. Per-section state: phase (cleaned/prepped/planted/established), density style, plant list, site labels. Includes the lightweight **outcome log** (thrived / struggled / died / moved) that feeds next-season recommendations. Data model sketch in [sunshower/tech-stack.md → User data](sunshower/tech-stack.md); sequenced behind Supabase setup (*App / Database*).
- 🚧 **Three.js landing/hero scene for the garden app entry page.** Persistent canvas + Phase-1-themed scene (rain / sun / weed tufts / shovel hotspot) shipped in commit `35d4422` — [src/app/sunshower/Scene.tsx](../src/app/sunshower/Scene.tsx) renders at the route layout so it doesn't remount on navigation. Responsive pass shipped in PR #16 (2026-05-16): desktop keeps the immersive fixed scene; mobile becomes a scrollable document; a `backdrop`-style Scene keeps ambient sun/rain/ground continuous across the breakpoint (no atmosphere pop), with fluid `clamp()` type. **Landing declutter 2026-07-06:** the interactive shovel hotspot + "cleanup plan" pill and the plant-selector / site-inventory quicklinks were removed — the [dropdown](../src/app/sunshower/YardStateRouter.tsx) ("How's your yard looking today?") is now the sole entry to the tools (it gained a "Ready to pick plants → Plant selector" row), and `Scene` reduced to the ambient backdrop (no shovel / no `variant` prop). Rationale: cut homepage clutter while functionality is built; artistic scene work resumes after. Remaining: stylized native CA flora + pollinator motif (bees, butterflies, hummingbirds) — current scene is cleanup-flavored, not yet the broader pollinator visual identity. Constraints: single canvas, lazy-loaded, mobile-friendly, mind LCP/bundle budget. Avoid generic "vibey portfolio" aesthetic.
- 🧊 **Visible-path navigation across the four phases** *(📋→🧊 2026-07-05 — Luc: hold off for now; selector build takes priority)*. Render Cleanup → Selection → Planning → Care as a literal trail (stepping stones / waypoint markers) inside the `/sunshower` three.js scene. Each stop is a clickable hotspot routing to its phase; camera tweens on linear traversal, direct jumps short-circuit to the target. Persistent-canvas plumbing already landed (above); shovel currently routes to cleanup — pattern proven. Pair with a HUD breadcrumb / chapter-overlay so deep-linked users can jump without learning the metaphor. Reference experience: [persepolis.getty.edu](https://persepolis.getty.edu/). Pattern + inspiration sites in [planning/sunshower/tech-stack.md → Navigation pattern](sunshower/tech-stack.md#navigation-pattern-planned) and [REFERENCES.md](sunshower/REFERENCES.md).
- 🧊 **Grown-in 3D view — separate requirement (split out of the bed-planner spec at sign-off, Luc 2026-07-06).** Read-only r3f rendering of a `GardenPlan` at mature size: massing archetypes (mounds / tufts / vase shrubs / canopy trees) scaled to `height/width_ft_max`, bloom-season tinting synced to the season scrubber, sightline camera bookmarks from `SiteProfile.sightlines` ("stand at your kitchen window"), stretch age slider. The "3D earns its weight" flagship moment — but the bed-planner stage is for planning the layout, so it ships separately. Activates once bed-planner placements + season scrubber (units E/G) exist; consumes them unchanged — nothing in the planner MVP depends on it. Full sketch preserved in [sunshower_bed_planner_spec.md → Explicitly deferred](sunshower_bed_planner_spec.md). Constraints: dynamic import, never SSR'd, instanced meshes for matrix fills, `/sunshower` LCP budget.

### Beginner-gardening ideas (added 2026-05-15)

Surfaced from the 4-article beginner-gardening ingest — see [planning/sunshower/CONTEXT.md → Ideas surfaced from beginner-gardening ingest](sunshower/CONTEXT.md#ideas-surfaced-from-beginner-gardening-ingest-2026-05-15) for the narrative rationale. The homepage yard-state router shipped (PR #16 → archive).

- 🧊 **Site-inventory fast-follows.** Deferred from the shipped walkthrough MVP (see [sunshower_site_inventory_mvp.md → Explicitly deferred](sunshower_site_inventory_mvp.md#explicitly-deferred-fast-follows-keep-on-the-backlog)): sketch/photo upload (needs the Supabase storage decision), zone-painting canvas (Phase-3 bed planner), profile export (print/share/JSON), multi-yard + per-section profiles (density-sectioning direction), Supabase `site_profiles` migration when auth lands.
- 🧊 **Site-inventory computed-sun upgrade (M2 "living inventory" — deferred, was 2026-07-03's "next functional build").** *Narrowed 2026-07-06: the computed first pass — suncalc + shadow projection + the S0 feasibility spike — moved into the bed-planner build as its sun/shade timelapse (unit H). What remains here is the living-layer half: observation check-ins, per-datum confidence, `'observed'` labels upgrading `'simulated'`/`'stated'` in place.* The shipped 8-step walkthrough (`feat/site-inventory-mvp`) captures sun zones as a *user-labeled list*. This item is the heavier upgrade Luc specced 2026-07-03 before the lighter walkthrough shipped: turn the inventory into a **living layer with per-datum confidence** rather than a one-time form. **Core design problem — inventory is time-distributed** (sun shifts across the day, shade across seasons; gardeners traditionally observe a full year). Resolution: (a) session 1 captures everything knowable at once; (b) **sun gets a computed first pass** — solar position is deterministic from lat/lng + date, so with user-traced obstructions (house footprint + rough height, fences, trees + deciduous flag) the app simulates the shade map for any hour/season immediately (candidate lib: `suncalc`, MIT); (c) opt-in hourly-photo + solstice check-ins override simulated values and raise confidence; (d) downstream fit verdicts consume confidence as bands ("likely fits — confirm afternoon summer shade"), never a completeness gate. Needs a feasibility spike (shadow-projection accuracy, client-side perf) before build. **The [Phase-3 bed planner](sunshower_bed_planner_spec.md) is deliberately built to absorb this later** — its `Obstruction` model already captures heights + `deciduous`, its section labels carry a `sunSource: 'stated' | 'simulated' | 'observed'` seam, and (as of the 2026-07-06 re-scope) it ships the simulation engine itself. Sequenced after the bed planner, not ahead of it. **Editor decisions already made (Luc 2026-07-03, from the superseded M1+M2 editor spec — preserved here so the old `docs/density-sectioning-roadmap` branch can be deleted):** obstruction heights via story presets (1 story ≈ 12 ft, 2 ≈ 22 ft) + optional free-feet override; yard-size cap ~10,000 sq ft ("map one area at a time" above it, revisit after the perf spike); section labels auto-derive from layer cells + one-tap confirm (label inherits cell confidence); staleness nudge fires only when a stored yard has an observed season *and* an unobserved one — dismissible banner on the `/sunshower` landing plus a persistent marker in the editor.
- 🧊 **Parallel-tracks UX — passive mapping + active in-yard removal.** Keep both kinds of tasks live at once: a user can map their landscape at the kitchen table or phone-in-hand at the window while actively pulling weeds outside. Concretely: a "what to do indoors today" vs. "what to do outdoors today" split in the cleanup-plan + site-inventory checklists, or a unified task list with venue tags. Cardboard / black-plastic suppression of un-worked beds (see [vault/concepts/planting-technique.md](../vault/concepts/planting-technique.md)) is the bridge — passive suppression runs while the user works elsewhere. Shape this further when the homepage router + site-inventory walkthrough land.
- 📋 **Copy pass — friendly-to-natives, not aggressive-to-weeds.** Lean cleanup-plan + landing copy toward *supporting native plants and pollinators* rather than *destroying invasives*. The plant-classification distinction (Cal-IPC invasive vs. common residential weed vs. native) is the lever — common weeds shouldn't carry the same urgency-language as Cal-IPC High invasives. Affects [src/app/sunshower/cleanup-plan/page.tsx](../src/app/sunshower/cleanup-plan/page.tsx) section headings, `SummaryCard` labels, and the landing page copy. Reference: [vault/concepts/wildlife-coexistence.md](../vault/concepts/wildlife-coexistence.md).
- 🧊 **Phase 4 (Care) — wiki + app content gap** *(📋→🧊 2026-07-05 — Luc: hold off for now; selector build takes priority)*. Empty Care phase today + no in-app destination for users with established yards. Two parallel tracks: **(a) wiki** — ingest a CA-native care source (watering for natives, feeding caveats, pruning timing including pollinator-overwintering, mulch timing); the generic care content in [vault/concepts/watering.md](../vault/concepts/watering.md), [vault/concepts/composting.md](../vault/concepts/composting.md), and [vault/concepts/pruning.md](../vault/concepts/pruning.md) all carry "may not apply to CA natives" caveats currently. **(b) app** — Phase 4 landing destination, even if just a "coming soon + interim links to existing wiki pages." Homepage yard-state router will surface this gap fast once it ships (an "established" answer has nowhere to go today).

### Garden-planning-thread ideas (added 2026-07-03)

Surfaced from the r/NativePlantGardening planning-thread ingest — see [vault/synthesis/garden-planning-flow-signals.md](../vault/synthesis/garden-planning-flow-signals.md) for the evidence and full signal list. Community-consensus evidence tier (single thread) — treat as hypotheses to corroborate. The density/sectioning/saved-plan direction items are tracked above, not here.

- 🧊 **Bloom-calendar view.** Month-by-month "what's blooming" storyboard generated from `bloom_season:` on the user's shortlist (data shipped for all 150 natives). Cheap, visually rewarding, and doubles as a **pollinator forage-gap detector** — a month with nothing blooming is a month with nothing to eat. Gardeners hand-build this today in Google Slides.
- 🧊 **Garden recipes / starter kits.** Pre-composed South Bay plant sets from data in hand (`native.communities` theme + `native.companions` pairing + `bloom_season` succession + spacing math) — e.g. "24 plants per 100 sq ft, spring/summer/fall bloom, grasses included." Proven durable demand (arboretum plug sets, repeat purchases). The "easy mode" on-ramp for users who find the full selector overwhelming.

## Scrapaholic retirement

The `/scrapaholic` project is archived. `main` carries only active project links and code; scrapaholic is preserved on the `archive/scrapaholic` branch. Branch cut + code removal + CLAUDE.md cleanup are done (→ archive).

- 📋 **`scrapaholic.lucttang.dev` subdomain decision** — DNS still points at the old route. Options: take down in Vercel, redirect to a static archive notice, or point at the archive branch's deploy. Followups in commit `dd33e86` message also include dropping `products` / `certification_cache` / `scrape_cache` / `brand_reputation` tables in Postgres and removing `SCRAPAHOLIC_PASSWORD` / `FIRECRAWL_API_KEY` / `GEMINI_API_KEY` / `APIFY_API_TOKEN` env vars in Vercel.

## Tooling

Convention settled (2026-05-17): **committed, re-runnable pipeline scripts live in [vault/scripts/](../vault/scripts/)** (UC IPM, WRIC, Calscape, iNat photos, vault lint); **`.firecrawl/` is gitignored scratch** (raw scrape output, job TSVs, caches). Pipeline conventions codified in the `sunshower-scrape` skill ([.claude/skills/](../.claude/skills/)). Calscape / WRIC / iNat pipelines are done (→ archive).

- 📋 Promote the legacy Cal-IPC one-offs (`.firecrawl/build-plant-pages.py`, `build-jobs.sh`, `run-scrape.sh`, `aggregate-stats.py`) into `vault/scripts/` if Cal-IPC pages ever need regeneration (e.g. richer body content from WRIC) — the only pipeline still outside the convention.
- 📋 **`lint_vault.py` — accepted-contradiction convention (raised by the 2026-07-05 lint pass).** The script counts *any* `Contradicts`/`[!warning]` callout as a hard defect, but the three current hits (right-plant-right-place, designed-plant-communities, plant-spacing) are the *deliberate, reconciled* method-vs-palette seams from the Post-Wild-World ingest — the pages themselves say "deliberate design seam, not an unresolved conflict." Needs a marker the script treats as closed (e.g. a `resolved:` line inside the callout, or a page-level `contradictions: reconciled` frontmatter key) so the exit code stays meaningful for CI. Until then the script exits 3 on a healthy vault.

## Documentation

[Project home](sunshower/), [vault schema](../vault/CLAUDE.md), and this backlog are established (→ archive). Completed work is now split into [sunshower_backlog_archive.md](sunshower_backlog_archive.md).

- 🧊 Architecture decision record (ADR) folder — if we accumulate enough non-obvious design decisions to warrant it.

## Open questions

- ❓ **Microclimate tagging within Central West.** Coastal CW vs South Bay foothills CW vs inland Diablo Range CW are ecologically distinct. Should this live in a `microclimate:` frontmatter field, in sub-region pages, or be left to body prose? *(The Calscape `native:` block introduced `native.communities`/`sunset_zones` — likely the de-facto resolution; confirm and close, or restate what's still open.)*
- ❓ **Cultivar vs species handling.** When natives come in, cultivars often have reduced pollinator value. Should each cultivar get its own plant page, or sit as variants under the species page? *(Calscape rollout note claims the `native:` block + `is_cultivar` resolves this — confirm against the shipped data and close if so.)*
